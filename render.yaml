services:
  - type: web
    name: biosense-ml-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --bind 0.0.0.0:$PORT
    envVars:
      - key: THINGSPEAK_CHANNEL_ID
        value: "3317281"
      - key: THINGSPEAK_READ_API_KEY
        value: "GE7EN76H12BQVULN"
